import { IType, IProvider } from '@/interfaces/Product.interface'

export default function ClientFilters ({ types, providers }: {types: IType[], providers: IProvider[]}) {
  return (
    <h1>asdf</h1>
  )
}
